# Core (probes, data generation, training, evaluation)
torch>=2.9          # developed on 2.10.0+cu128
transformers>=4.57  # developed on 4.57.6
httpx>=0.28         # zero-shot probes against a local vLLM server
pytest>=8.0         # data/test_datagen.py
numpy>=1.24        # dense-index reuse and retrieval data generation

# Optional — only needed for the game-domain comparison rollouts
# (NanoJev's ViZDoom basic / predict_position environments):
# vizdoom>=1.2

# Optional — only needed for the 4B LoRA scaling line
# (training/lora4b_model.py, rag_eval/run_iterative.py):
# peft>=0.13
